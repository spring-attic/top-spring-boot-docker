pipeline {
    agent {  label 'master' }
    stages {
        stage('Build jar using maven') {
            steps {
                script {
                    sh """ ./mvnw clean install """
        }}}
        
    }
}